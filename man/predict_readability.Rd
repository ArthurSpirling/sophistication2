% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_readability.R
\name{predict_readability}
\alias{predict_readability}
\title{Predict readability scores}
\usage{
predict_readability(model, newdata, measure = "predicted")
}
\arguments{
\item{model}{a fitted model object (e.g., from \code{lm} or other regression)}

\item{newdata}{data.frame of covariates for prediction}

\item{measure}{readability measure name (for labeling)}
}
\value{
a data.frame with predicted readability scores
}
\description{
Predicts readability scores for new texts based on a trained model.
}
\examples{
\dontrun{
# Train a simple model
train_data <- data.frame(
  readability = rnorm(100, mean = 50, sd = 10),
  n_tokens = rnorm(100, mean = 100, sd = 20),
  ttr = rnorm(100, mean = 0.5, sd = 0.1)
)
model <- lm(readability ~ n_tokens + ttr, data = train_data)

# Predict on new data
new_data <- data.frame(
  n_tokens = c(90, 110),
  ttr = c(0.45, 0.55)
)
predict_readability(model, new_data)
}
}
